namespace OmegaProjectGame
{
    /// <summary>
    /// enums that represent a direction that the board can/will shift 
    /// </summary>
    public enum Directions
    {
        NoMove,
        Up,
        Right,
        Down,
        Left
    }
}